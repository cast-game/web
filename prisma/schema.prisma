generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Round {
  id              Int      @id
  title           String
  channelId       String
  contractAddress String
  end             DateTime
}
